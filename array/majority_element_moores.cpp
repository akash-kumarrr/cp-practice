#include <bits/stdc++.h>

using namespace std;

typedef vector<int> vi;

#define lp(i, e) for (int i=0; i<e; i++)
#define pb push_back

int main(){
    int a, b, x, y;
    vi v;
    if (!(cin >> x) || !x) return 0;

    while(x--) {
        cin >> a;
        v.pb(a);
    }

    int max_count=0, max_val=0;

    lp(i, v.size()){
        if(v[i] == )
    }
}