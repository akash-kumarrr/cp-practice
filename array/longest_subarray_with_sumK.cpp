//find the longest subarray with sum of k

#include <bits/stdc++.h>
#define pb push_back

using namespace std;

typedef vector<int> vi;

int main() {

    int a, b, c, x, y, s;
    vi v;
    if(!(cin >> x) || !x) return 0;
    y = x;
    while (x--){
        cin >> a;
        v.pb(a);
    }
}