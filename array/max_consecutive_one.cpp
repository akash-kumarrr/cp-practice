//maximum consecutive ones in an array

#include<bits/stdc++.h>

using namespace std;

#define _(i, e) for (int i=0; i<e;i++)
#define pb push_back

typedef vector<int> vi;

int main(){

    int a, b, c, x, y;
    vi v;

    if(!(cin >> c) || !c) return 0;

    x=c;

    while (c--) {
        cin >> a;
        v.pb(a);
    }
    
    int i=0;

    while (i < x) {
        
    }
}