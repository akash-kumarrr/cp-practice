#include <bits/stdc++.h>

using namespace std;

#define le(i, e) for (int i=0; i<e; i++)

struct Node {
    int coeff;
    Node* next;
}

int main() {

    int deg;
    cout << "enter max degree of poly : ";
    cin >> deg;

    

    le(i, deg) {

    }
}